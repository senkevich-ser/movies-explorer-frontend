import React from 'react';
import Logo from '../Logo/Logo';
import './Header.css';

function Header() {
  return (
    <header className="Header">
      <Logo />
    </header >
  )
};

export default Header;